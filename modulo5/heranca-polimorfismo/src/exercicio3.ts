import { Customer } from "./customer";


// exercicio 3 Agora, imprima **todas as informações possíveis** da instância que você criou: o id, o nome,
// o email, o valor total de compra (`purchaseTotal`) e o cartão de crédito (`creditCard`). 
//Perceba que as propriedades públicas da classe pai (`User`) foram "herdadas" pela classe filha
// (`Customer`).

console.log(Customer);

//a) *Seria possível imprimir a senha (`password`) atrelada a essa instância?* *Por quê?*

